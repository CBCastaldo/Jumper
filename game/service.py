class Service:
    #The main purpose of this program would be to print everything

    def read_letter(self, prompt): #Prints whichever prompt is in the parameters.
        return print(prompt)
    
    def print_list(self, list): #Prints Parachute
        for index in range(len(list)):
            print(f"{list[index]}")
   

    